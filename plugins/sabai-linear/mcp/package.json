{
  "name": "sabai-linear-mcp",
  "version": "1.0.0",
  "description": "Linear MCP server — token-based auth with @linear/sdk",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "start": "npx tsx index.ts",
    "build": "npx esbuild index.ts --bundle --platform=node --target=node18 --outfile=dist/server.cjs --format=cjs --minify"
  },
  "dependencies": {
    "@linear/sdk": "^37.0.0",
    "@modelcontextprotocol/sdk": "^1.27.1"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
