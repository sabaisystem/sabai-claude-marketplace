{
  "name": "sabai-discord-mcp",
  "version": "1.0.0",
  "description": "Discord MCP server — REST-only, no WebSocket",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npx esbuild index.js --bundle --platform=node --target=node18 --outfile=dist/server.cjs --format=cjs --minify"
  },
  "dependencies": {
    "@discordjs/rest": "^2.6.0",
    "@modelcontextprotocol/sdk": "^1.27.1",
    "discord-api-types": "^0.38.40"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}
